# compose.yaml
# Avvia il servizio "app" del chatbot RAG per lo studio legale

services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./documenti:/app/documenti