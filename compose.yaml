# compose.yaml
# Avvia il servizio "api" del chatbot dello studio legale

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs